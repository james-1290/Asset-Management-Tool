server:
  port: 5115

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/assetmgmt?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    # Disable Open Session In View: the Hibernate session is bound to the
    # transaction, not the HTTP request. Read endpoints that build DTOs from
    # lazy relations run within @Transactional(readOnly = true); DTOs are plain
    # data by the time serialization runs. This releases DB connections earlier
    # and surfaces lazy-load mistakes instead of masking them.
    open-in-view: false
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
        # Batch lazy/secondary to-one loads so a page of rows resolves its
        # relations in a few IN(...) queries instead of one-per-row (N+1) on
        # exports and sub-resource lists that don't fetch-join.
        default_batch_fetch_size: 100
        type:
          preferred_uuid_jdbc_type: CHAR
  flyway:
    enabled: true
    locations: classpath:db/migration
    baseline-on-migrate: true
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  mail:
    host: ""
  autoconfigure:
    exclude: org.springframework.boot.autoconfigure.mail.MailSenderAutoConfiguration
  jackson:
    serialization:
      write-dates-as-timestamps: false
    default-property-inclusion: non_null

jwt:
  key: ${JWT_KEY:ThisIsASecretKeyForDevelopmentPurposesOnly123!}
  issuer: AssetManagementApi
  audience: AssetManagementApp
  expiry-hours: 2

cors:
  origins: http://localhost:5173

saml:
  enabled: ${SAML_ENABLED:false}
  registration-id: ${SAML_REGISTRATION_ID:entra}
  entity-id: ${SAML_ENTITY_ID:asset-management-sp}
  acs-url: ${SAML_ACS_URL:http://localhost:5115/login/saml2/sso/entra}
  metadata-url: ${SAML_METADATA_URL:}
  signing-key: ${SAML_SIGNING_KEY:classpath:saml/saml-signing-key.pem}
  signing-cert: ${SAML_SIGNING_CERT:classpath:saml/saml-signing-cert.pem}
  default-role: ${SAML_DEFAULT_ROLE:User}
  frontend-origin: ${SAML_FRONTEND_ORIGIN:http://localhost:5173}

scim:
  enabled: ${SCIM_ENABLED:false}
  bearer-token: ${SCIM_BEARER_TOKEN:change-me-in-production}
  base-url: ${SCIM_BASE_URL:http://localhost:5115}

auth:
  local-login:
    enabled: ${LOCAL_LOGIN_ENABLED:true}

app:
  admin:
    password: ${ADMIN_PASSWORD:admin123}
  upload-dir: ${UPLOAD_DIR:./uploads}

springdoc:
  api-docs:
    enabled: ${SWAGGER_ENABLED:false}
    path: /v3/api-docs
  swagger-ui:
    enabled: ${SWAGGER_ENABLED:false}
    path: /swagger-ui.html
